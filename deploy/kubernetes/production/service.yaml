apiVersion: v1
kind: Service
metadata:
  name: pynomaly-api
  namespace: pynomaly-production
  labels:
    app: pynomaly-api
    component: backend
    environment: production
spec:
  type: ClusterIP
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: pynomaly-api
    component: backend
---
apiVersion: v1
kind: Service
metadata:
  name: pynomaly-web
  namespace: pynomaly-production
  labels:
    app: pynomaly-web
    component: frontend
    environment: production
spec:
  type: ClusterIP
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: pynomaly-web
    component: frontend
