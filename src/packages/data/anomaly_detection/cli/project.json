{
  "name": "cli",
  "sourceRoot": "src/packages/cli",
  "projectType": "library",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "buck2 build //src/packages/cli:cli",
        "cwd": "{workspaceRoot}"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "buck2 test //src/packages/cli:test-all",
        "cwd": "{workspaceRoot}"
      }
    },
    "test-commands": {
      "executor": "nx:run-commands",
      "options": {
        "command": "buck2 test //src/packages/cli:test-commands",
        "cwd": "{workspaceRoot}"
      }
    },
    "test-app": {
      "executor": "nx:run-commands",
      "options": {
        "command": "buck2 test //src/packages/cli:test-app",
        "cwd": "{workspaceRoot}"
      }
    },
    "test-config": {
      "executor": "nx:run-commands",
      "options": {
        "command": "buck2 test //src/packages/cli:test-config",
        "cwd": "{workspaceRoot}"
      }
    },
    "test-integration": {
      "executor": "nx:run-commands",
      "options": {
        "command": "buck2 test //src/packages/cli:test-integration",
        "cwd": "{workspaceRoot}"
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "buck2 build //src/packages/cli:lint",
        "cwd": "{workspaceRoot}"
      }
    },
    "dev": {
      "executor": "nx:run-commands",
      "options": {
        "command": "buck2 build //src/packages/cli:dev",
        "cwd": "{workspaceRoot}"
      }
    }
  },
  "tags": [
    null,
    null
  ]
}