{
  "name": "api",
  "sourceRoot": "src/packages/api",
  "projectType": "library",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "buck2 build //src/packages/api:api",
        "cwd": "{workspaceRoot}"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "buck2 test //src/packages/api:test-all",
        "cwd": "{workspaceRoot}"
      }
    },
    "test-endpoints": {
      "executor": "nx:run-commands",
      "options": {
        "command": "buck2 test //src/packages/api:test-endpoints",
        "cwd": "{workspaceRoot}"
      }
    },
    "test-security": {
      "executor": "nx:run-commands",
      "options": {
        "command": "buck2 test //src/packages/api:test-security",
        "cwd": "{workspaceRoot}"
      }
    },
    "test-middleware": {
      "executor": "nx:run-commands",
      "options": {
        "command": "buck2 test //src/packages/api:test-middleware",
        "cwd": "{workspaceRoot}"
      }
    },
    "test-integration": {
      "executor": "nx:run-commands",
      "options": {
        "command": "buck2 test //src/packages/api:test-integration",
        "cwd": "{workspaceRoot}"
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "buck2 build //src/packages/api:lint",
        "cwd": "{workspaceRoot}"
      }
    },
    "dev": {
      "executor": "nx:run-commands",
      "options": {
        "command": "buck2 build //src/packages/api:dev",
        "cwd": "{workspaceRoot}"
      }
    }
  },
  "tags": [
    null,
    null
  ]
}