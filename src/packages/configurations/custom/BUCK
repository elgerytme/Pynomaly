# BUCK file for custom_configurations package

python_library(
    name = "custom_configurations",
    srcs = glob(["src/**/*.py"]),
    visibility = ["PUBLIC"],
    deps = [
        # Add dependencies here
    ],
)
