# Neuro-Symbolic AI Package - Standardized Buck2 Configuration
# Domain: ai
# Package: neuro_symbolic
# Type: ml

load("//tools/buck:python_package.bzl", "python_package")

load("//tools/buck:python_package.bzl", "ml_package")

ml_package(
    name = "neuro_symbolic",
    frameworks = ["sklearn"],  # Adjust as needed
    visibility = ["PUBLIC"],
)
