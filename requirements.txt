# Core Required Dependencies (minimal set)
pyod>=2.0.5,<3.0.0
numpy>=1.26.0,<2.3.0
pandas>=2.2.3,<3.0.0
polars>=1.19.0,<2.0.0

# Core Architecture Dependencies
pydantic>=2.10.4,<3.0.0
structlog>=24.4.0,<25.0.0
dependency-injector>=4.42.0,<5.0.0

# Note: For additional functionality, install with extras:
# pip install pynomaly[api]     # Web API functionality
# pip install pynomaly[cli]     # Command-line interface
# pip install pynomaly[server]  # API + CLI + basic features
# pip install pynomaly[production]  # Production-ready features
# pip install pynomaly[all]     # All optional dependencies